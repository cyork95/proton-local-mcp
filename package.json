{
  "name": "proton-standard-notes-mcp",
  "version": "1.0.0",
  "description": "Self-hosted MCP server connecting Claude to Google (Gmail, Calendar, Drive, Tasks, Health) and Standard Notes",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node --dns-result-order=ipv4first server.js",
    "test": "node --dns-result-order=ipv4first test-server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "better-sqlite3": "^12.10.0",
    "dotenv": "^16.4.0"
  }
}
